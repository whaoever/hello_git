sudo: required
language: cpp
branches:
    only:
        - 'master'

compiler:
    - clang
    - gcc

install:
    - if ["$CXX" == "g++"]; then sudo apt-get install -qq g++-4.8; fi
    - if ["$CXX" == "g++"]; then export CXX="g++-5"; fi
script:
    - make
